import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex:1,
    flexDirection: "row", 
    position:"relative",
    left: 150,
  },
  icon: {
    marginLeft: 8,
  },
});

export default styles;
